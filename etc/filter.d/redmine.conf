[Definition]

# Rate-limit repeated Redmine login attempts.
# This matches login submissions, not specifically failed authentication.
failregex = ^<HOST> .* "POST /login(?:\?.*)? HTTP/.*" 200

ignoreregex =
